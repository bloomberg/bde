// Include header files of components required for testing, so build tool 
// recompiles when there are changes to these components. Build tool will not 
// recompile if included header files only in the test files and not in the
// component header file. Component .cpp requires access to .h file.


#include <bdetst_stlvector.h>
