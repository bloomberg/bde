// bdetstdb_usage_user.csc2 schema for table "user" table from usage examples
// Note that this table in the test database should not be modified by the
// test driver and should include the following data
//..
// comdb2sql testdb 'INSERT INTO user (last_name,first_name,zipcode)
//                   VALUES("Doe", "John", 11021)'
// comdb2sql testdb 'INSERT INTO user (last_name,first_name,zipcode) 
//                   VALUES("Smith", "Jane", 10001)'
// comdb2sql testdb 'INSERT INTO user (last_name,first_name,zipcode) 
//                   VALUES("Brown", "Sally", 10022)'
//..


// This is the "ondisk" tag; it defines the data actually stored on disk
tag ondisk
{
    cstring  last_name[32]  
    cstring  first_name[32]  
    int      zipcode
}

tag "fullrecord"
{
    cstring  last_name[32]  
    cstring  first_name[32]  
    int      zipcode
}

keys
{
    "USER_KEY"   = last_name + first_name + zipcode
dup "ZIP_KEY"    = zipcode
}

